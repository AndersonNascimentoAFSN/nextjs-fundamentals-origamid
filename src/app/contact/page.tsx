import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contato",
  description: "Página sobre o contato",
};

export default function ContactPage() {
  return (
    <div>Contato</div>
  )
}
